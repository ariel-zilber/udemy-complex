sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t coffee_coder/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true coffee_coder/react-test npm test

after_success:
  - docker build -t coffee_coder/multi-client ./client
  - docker build -t coffee_coder/multi-nginx ./nginx
  - docker build -t coffee_coder/multi-server ./server
  - docker build -t coffee_coder/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push coffee_coder/multi-client
  - docker push coffee_coder/multi-nginx
  - docker push coffee_coder/multi-server
  - docker push coffee_coder/multi-worker
